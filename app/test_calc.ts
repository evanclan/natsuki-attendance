import { updateAttendanceRecord } from './src/app/admin/attendance-actions/actions'

const res = async () => {
    // Need a valid ID and personId from DB
    // 1366 is an ID from earlier query
    // Let's just create a custom script that does EXACTLY what updateAttendanceRecord does.
}
